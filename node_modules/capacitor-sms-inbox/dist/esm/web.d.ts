import { WebPlugin } from '@capacitor/core';
import type { PermissionStatus, Projection, SMSFilter, SMSInboxReaderPlugin, SMSObject } from './definitions';
export declare class SMSInboxReaderWeb extends WebPlugin implements SMSInboxReaderPlugin {
    getRawSMSList(_options: {
        filter?: SMSFilter | undefined;
    }): Promise<{
        rawSmsList: any;
    }>;
    getCount(_options: {
        filter: SMSFilter;
    }): Promise<{
        count: number;
    }>;
    getSMSList(_options: {
        projection: Projection;
        filter: SMSFilter;
    }): Promise<{
        smsList: SMSObject[];
    }>;
    checkPermissions(): Promise<PermissionStatus>;
    requestPermissions(): Promise<PermissionStatus>;
}
