{
  "name": "pb-expense-tracker",
  "version": "1.0.0",
  "description": "People's Bank Expense Tracker - Auto-reads bank SMS",
  "main": "index.html",
  "scripts": {
    "dev": "npx serve www -p 3000 --no-clipboard",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:build": "npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/preferences": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0",
    "capacitor-sms-inbox": "^1.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "sharp": "^0.34.5",
    "typescript": "^6.0.3"
  }
}
